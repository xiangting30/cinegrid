import { ShotPreset } from './types';

export const SHOT_PRESETS = [
  ShotPreset.ELS,
  ShotPreset.LS,
  ShotPreset.MLS,
  ShotPreset.MS,
  ShotPreset.MCU,
  ShotPreset.CU,
  ShotPreset.ECU,
  ShotPreset.LOW_ANGLE,
  ShotPreset.HIGH_ANGLE,
];

export const INITIAL_GRID_SIZE = 4; // 2x2 grid by default (if we were doing static array)
